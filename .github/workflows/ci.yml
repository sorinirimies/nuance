name: test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    name: test (${{ matrix.os }}, nu ${{ matrix.nu }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        nu: ["0.111.0", "0.114.1"]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Nushell
        uses: hustcer/setup-nu@v3
        with:
          version: ${{ matrix.nu }}

      - name: Install bats
        shell: bash
        run: |
          if [ "$(uname)" = "Darwin" ]; then
            brew install bats-core
          else
            sudo apt-get update && sudo apt-get install -y bats
          fi

      - name: Run nushell tests
        shell: bash
        run: nu test.nu

      - name: Run bash tests
        shell: bash
        run: bats test.bats

      - name: Verify install (symlink into autoload)
        shell: bash
        run: |
          nu install.nu
          DIR="$(nu -n -c '$nu.user-autoload-dirs | get 0')"
          test -e "$DIR/nushell-prompt.nu" && echo "✓ installed at $DIR/nushell-prompt.nu"

      - name: Install bats
        shell: bash
        run: npm install -g bats

      - name: Run CLI tests (bats)
        shell: bash
        run: bats test.bats
